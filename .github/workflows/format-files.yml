name: format files
run-name: format
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: github
    steps:
      - run: npm install
      - run: npm run lint
      - run: npm run format
      - run: npm run test
